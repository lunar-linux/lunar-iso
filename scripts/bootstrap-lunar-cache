#!/bin/bash

set -euo pipefail

SCRIPT_DIR="$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)"

echo "bootstrap-lunar-cache is deprecated; using source-based bootstrap fetch instead." >&2
"$SCRIPT_DIR/bootstrap-fetch-sources" bootstrap
