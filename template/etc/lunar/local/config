          GCCVER=3
           FUZZY=off
         ARCHIVE=off
         AUTOFIX=off
       AUTOPRUNE=off
    MAIL_REPORTS=off
        PRESERVE=on
           SOUND=off
         SUSTAIN=on
    VIEW_REPORTS=off
          VOYEUR=on
            REAP=on
         GARBAGE=on
           TMPFS=off
    XFCE4_PREFIX=/usr
       QT_PREFIX=/usr
      KDE_PREFIX=/usr
          KDEDIR=/usr
           QTDIR=/usr/lib
 BUILD_DIRECTORY=/var/build
   AUTORESURRECT=off
     KEEP_SOURCE=off
         VERBOSE=off
ZLOCAL_OVERRIDES=on
           COLOR=on
   CVS_THRESHOLD=120
   PROBE_EXPIRED=on
