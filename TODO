* grsecurity kernels are not grsec enabled
* post-install bug: lin linux-2.6* doesn't adjust lilo.conf
* add edvanced options:
	- extract && make oldconfig a full kernel source on target
	- copy cache tarballs to /var/cache/lunar
